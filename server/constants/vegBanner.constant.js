// Shared with the home feed (home.service.js) and the search "Popular right now"
// surface (search.controller.js) — one copy so the two screens' veg-mode
// confirmation strips never drift apart.
export const VEG_BANNER_TEXT = 'Pure veg mode is on — showing only vegetarian food';
